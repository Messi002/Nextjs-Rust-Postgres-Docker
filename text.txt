create the compose.yaml file

then 
- docker compose up -d if 
and error occurs 
- docker compose down

run 
- docker ps -a 
to check all containers and shut down unwanted ones using 
- docker stop elastic_perlman
- docker rm elastic_perlman

To check if everything is alright and to be inside the container:
 docker exec -it db psql -U postgres
